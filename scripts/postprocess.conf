# BioHEL-RPE postprocess configuration
# Operator policies (keep exactly as requested)

operator 1 policy cl
operator 2 policy pr
operator 3 policy cl2
operator 4 policy pr
operator 5 policy sw

# Optional: keep stats concise (END) vs verbose (ALL)
trainset stats enabled END
testset stats enabled END
